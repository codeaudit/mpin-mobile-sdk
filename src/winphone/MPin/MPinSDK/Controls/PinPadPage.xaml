<Page
    x:Class="MPinSDK.Controls.PinPadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MPinSDK.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mPin="using:MPinSDK.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>        
    </Page.Resources>
    <Grid x:Name="PinLayoutGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="PinMailTB" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Left" 
                   FontWeight="{ThemeResource PhoneButtonFontWeight}"
                   FontSize="{ThemeResource TextStyleMediumFontSize}"
                   Margin="12"/>
        <TextBlock x:Name="IdentityMailTB"
                   Grid.Row="1"
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Left" 
                   FontSize="{ThemeResource TextStyleMediumFontSize}"
                   Margin="12"/>
        
        <mPin:PinPadControl x:Name="PinPad" Grid.Row="2" VerticalAlignment="Bottom" />
        
    </Grid>
</Page>
