<Page
    x:Class="MPinDemo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MPinDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mPin="using:MPinSDK.Controls"
    mc:Ignorable="d"
    Style="{StaticResource PageStyle}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Frame x:Name="MainFrame" Grid.Row="1" Margin="0,25,0,0"/>
        <Border x:Name="StatusBorder" Grid.Row="2" Margin="-12, 0, -12, 0" Visibility="Collapsed">
            <TextBlock x:Name="StatusBlock" Style="{StaticResource StatusStyle}" />
        </Border>
    </Grid>
</Page>
