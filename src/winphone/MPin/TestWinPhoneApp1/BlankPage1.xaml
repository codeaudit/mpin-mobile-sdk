<Page
    x:Class="TestWinPhoneApp1.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestWinPhoneApp1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Pivot x:Name="MainPivot">

            <PivotItem Header="Services" x:Name="ServicesPivotItem">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" >
                        <Button Content="Add" Click="AddService_Click" Margin="2"/>
                        <Button Content="Delete" Click="DeleteService_Click" Margin="2"/>
                        <Button x:Name="TestServiceButton" Content="Test Backend" Click="TestBackend_click" Margin="2" IsEnabled="False" />
                    </StackPanel>
                    <TextBlock Text="Please, choose а configuration" Grid.Row="1"/>
                    <ListBox x:Name="ServicesList" Grid.Row="2" SelectedItem="{Binding CurrentService, Mode=TwoWay}" SelectionChanged="Services_SelectionChanged" >
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding Title}" />
                                    <TextBlock Text="{Binding BackendUrl}" FontWeight="Light" Grid.Row="1"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </PivotItem>

            <PivotItem Header="Users" x:Name="UsersPivotItem">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="1">
                        <Button Margin="2" Content="Add" Click="AddUser_Click" x:Name="AddUserButton"/>
                        <Button Margin="2" Content="Delete" Click="DeleteUser_Click" x:Name="DeleteUserButton"/>
                        <Button Margin="2" Content="Select" Click="Authenticate_Click" x:Name="AuthenticateButton"/>
                    </StackPanel>
                    <ListBox x:Name="UsersList"  SelectionChanged="UsersList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Red" BorderThickness="1" Margin="2">
                                    <TextBlock Text="{Binding Id}" />
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </PivotItem>

        </Pivot>

    </Grid>
</Page>
