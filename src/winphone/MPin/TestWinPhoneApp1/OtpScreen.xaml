<Page
    x:Class="MPinDemo.OtpScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MPinDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    
    <StackPanel>
        <TextBlock x:Uid="OtpTitle" Style="{StaticResource TitleStyle}"/>
        <TextBlock Text="{Binding CurrentUser.Id, Mode=TwoWay}" Style="{StaticResource IdentityTextBlock}"/>
        <TextBlock x:Uid="OtpTB" Style="{StaticResource MainTextBlock}" TextWrapping="WrapWholeWords"/>

        <TextBlock x:Uid="Otp2TB" Style="{StaticResource MainTextBlock}" FontSize="{ThemeResource TextStyleMediumFontSize}"/>
        
        <TextBlock Text="{Binding Otp.Otp, Mode=TwoWay}" FontSize="{ThemeResource TextStyleExtraExtraLargeFontSize}"/>
        <ProgressBar
                     x:Name="TtlProgressBar"
                     VerticalAlignment="Top"
                     IsIndeterminate="False" 
                     Maximum="{Binding Otp.TtlSeconds}" 
                     Value="{Binding ProgressPercent}"
                     />
    </StackPanel>    
</Page>
