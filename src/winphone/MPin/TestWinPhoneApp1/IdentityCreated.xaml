<Page
    x:Class="MPinDemo.IdentityCreated"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MPinDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Style="{StaticResource PageStyle}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
   >

    <Grid>
        <StackPanel>
            <TextBlock x:Uid="IdentityCreatedTitle" Style="{StaticResource TitleStyle}"/>
            <TextBlock x:Name="IdentityMail" Style="{StaticResource IdentityTextBlock}"/>
            <TextBlock x:Uid="IdentityCreatedNotification" TextWrapping="WrapWholeWords" Style="{StaticResource MainTextBlock}"/>
            <Button x:Uid="SignInButton" Click="SignInButton_Click"/>
            <Button x:Uid="IdentityListButton" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Page>
